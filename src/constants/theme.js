import { createTheme } from '@mui/material/styles';;

const theme = createTheme({
  palette: {
    primary: {
      main: '#E8222E',
      contrastText: '#000000',
    },
    secondary: {
      main: '#B8B8B8',
      contrastText: '#000000',
    },
  },
});

export default theme