import { createMuiTheme } from "@material-ui/core";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#E8222E',
      contrastText: '#000000',
    },
    secondary: {
      main: '#B8B8B8',
      contrastText: '#000000',
    },
  },
});

export default theme